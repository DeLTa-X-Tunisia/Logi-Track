# Configuration de la base de données MySQL (Laragon)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=logitrack
DB_PORT=3306

# Configuration du serveur
PORT=3002
NODE_ENV=development

# Configuration JWT
JWT_SECRET=change_this_to_a_secure_random_key
JWT_EXPIRES_IN=24h

# Configuration Socket.io / WebRTC
SOCKET_ENABLED=true
WEBRTC_ENABLED=false
